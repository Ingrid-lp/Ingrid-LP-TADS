# Ingrid-LP-TADS

Se você alterar algo no projeto:

git add .
git commit -m "alguma alteração"
git push

git config --global user.email "ingrid4519v@gmail.com"
git config --global user.name "ingrid4519v"
